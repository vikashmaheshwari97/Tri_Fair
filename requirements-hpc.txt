# Linux/CUDA runtime for Rocket.
# Torch is intentionally not pinned separately: vLLM must select a compatible build.

-r analysis/requirements-analysis.txt

promptolution[vllm]==2.2.3
datasets==5.0.0
huggingface-hub==1.22.0
pymoo==0.6.2
scikit-learn==1.9.0

# Validation/development tools
pytest==9.1.1
ruff==0.15.20